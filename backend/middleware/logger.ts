import type { Elysia } from 'elysia';

/**
 * Request Logger Middleware
 * Placeholder — enable logging here if needed for debugging.
 */
export function loggerMiddleware(app: Elysia) {
	return app;
}
